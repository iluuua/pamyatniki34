# VPS deployment — pending access

Target: pamyatniki34.ru (corrected owner domain), Hostkey 77.91.115.169.

Exact MAX profile links applied; Telegram retains the requested draft. Canonical validator passed.
SSH private/public fingerprints match, but root authentication is rejected on both default routing and en0. Hostkey Chrome tab requires sign-in. REG.RU zone is accessible and unchanged (A @ and www: 95.163.244.138).

HTTP Nginx bootstrap config is prepared, not installed. No VPS changes or certificate requests have occurred. DNS is held until the site can be verified on the server.

Required next action: owner signs in to Hostkey; then inspect server access, deploy build, point both DNS records to 77.91.115.169, issue and verify HTTPS plus renewal.

GitHub Pages update completed: commit 679432693e86109e43671fc2e9adcfd1ce85cc0f; action 34322061320 succeeded. Public HTTPS HTML matches dist exactly, including all six exact MAX links. Source implementation: ac26c90. No force push.
